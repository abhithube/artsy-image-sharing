{
  "name": "graphql-react-image-sharing",
  "version": "1.0.0",
  "description": "",
  "main": "server/src/server.ts",
  "scripts": {
    "start": "yarn --cwd server start",
    "dev": "concurrently \"yarn --cwd server dev\" \"yarn --cwd client start\"",
    "heroku-postbuild": "yarn --cwd server && yarn --cwd client && yarn --cwd client build"
  },
  "devDependencies": {
    "concurrently": "^6.1.0"
  },
  "author": "",
  "license": "ISC"
}
