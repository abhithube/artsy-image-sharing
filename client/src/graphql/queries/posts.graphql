query posts(
  $userId: Int
  $field: PostSortField! = CREATED_AT
  $direction: SortDirection! = DESC
  $limit: Int
  $page: Int
) {
  posts(
    userId: $userId
    orderBy: { field: $field, direction: $direction }
    limit: $limit
    page: $page
  ) {
    ...posts
  }
}
