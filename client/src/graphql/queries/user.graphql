query user($id: Int!) {
  user(id: $id) {
    id
    username
    avatarUrl
    posts(limit: 5) {
      ...posts
    }
    postCount
    favorites(limit: 5) {
      ...favorites
    }
    favoriteCount
    createdAt
  }
}
