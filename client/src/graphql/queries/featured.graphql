query featured(
  $field: PostSortField!
  $direction: SortDirection!
  $limit: Int
) {
  posts(orderBy: { field: $field, direction: $direction }, limit: $limit) {
    results {
      ...postDetails
    }
  }
}
