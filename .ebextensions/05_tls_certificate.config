commands:
  01_copy_certificate:
    command: |
      sudo rm -rf /etc/letsencrypt /etc/nginx
      sudo cp -r /data/letsencrypt /data/nginx /etc

      sudo systemctl reload nginx
    test: "test -d /data/letsencrypt"