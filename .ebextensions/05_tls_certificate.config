container_commands:
  01_copy_certificate:
    command: |
      sudo rm -rf /etc/letsencrypt
      sudo cp -r /data/letsencrypt /etc

      sudo cp /data/nginx.conf /etc/nginx
    test: "test -d /data/letsencrypt"